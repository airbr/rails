<h1>Articles</h1>

Our blog has:

<ul>
<li>
<%= Article.loud_count %> "Loud" (You can comment) articles and counting!
</li>
<li>
<%= Article.subdued_count %> "Subdued" articles and counting!
</li>
</ul>
<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <li>
        <%= link_to article.title, article %> - <%= article.status %>
      </li>
    <% end %>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
