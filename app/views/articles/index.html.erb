<div class="flex flex-col items-center">
<a href="/"><h1>Home</h1></a>

<p class="notice"><%= flash[:notice] %></p>
<p class="alert"><%= flash[:alert] %></p>
<br>
<% if current_user %>
  <li><%= link_to "Sign out", destroy_user_session_path, method: :get  %></li>
<% else %>
  <li><%= link_to "Sign up", new_user_registration_path %></li>
  <li><%= link_to "Log in", user_session_path %></li>
<% end %>
<br>
<br>
<h1>Articles</h1>
<br>
Our blog has:
<ul class="list-disc p-8">
<li>
<%= Article.loud_count %> "Loud" articles where users can comment,
</li>
<li>
<%= Article.subdued_count %> "Subdued" articles where we can observe,
</li>
<li>
and counting!
</li>
</ul>
<%= link_to "Make a New ☕ Post", new_article_path, {class: "underline"} %>
<br>

<ul class="list-disc p-8">
<li>
<%# <%= link_to @trending.title, @trending, {class: "underline"} %> is the Trending Topic with the Most Comments! %>
</li>
</ul>
<br>
<h4 class="pl-6">Loud articles:</h4>
<ul class="list-disc">
  <% @articles.each do |article| %>
    <% if article.status == "loud" %>
      <li>
        <%= link_to article.title, article, {class: "underline"} %>
        <%= article.comments.length %> comments
      </li>
    <% end %>
  <% end %>
</ul>
<h4 class="pl-6">subdued articles:</h4>
<ul class="list-disc pb-8">
  <% @articles.each do |article| %>
    <% if article.status == "subdued" %>
      <li>
        <%= link_to article.title, article, {class: "underline"} %>
      </li>
    <% end %>
  <% end %>
</ul>

</div>